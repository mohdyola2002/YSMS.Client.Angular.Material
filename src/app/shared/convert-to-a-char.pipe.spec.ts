import { ConvertToACharPipe } from './convert-to-a-char.pipe';

describe('ConvertToACharPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertToACharPipe();
    expect(pipe).toBeTruthy();
  });
});
